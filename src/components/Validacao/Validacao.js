import React, {useState} from 'react';
import { View} from 'react-native';

export default function Validacao() {

    const validacao = (email=string) => {
        const regexEmail = RegExp (/^[ifpr]$/)} 
    
    return (
        <View/>
    );
}